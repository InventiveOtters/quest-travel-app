<?xml version="1.0" encoding="utf-8" ?>
<manifest
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:horizonos="http://schemas.horizonos/sdk"
  xmlns:tools="http://schemas.android.com/tools"
  android:versionCode="1"
  android:versionName="1.0"
  android:installLocation="auto"
>
  
  <horizonos:uses-horizonos-sdk
    horizonos:minSdkVersion="69"
    horizonos:targetSdkVersion="69"
  />
  <!-- VR head tracking is required for immersive experience -->
  <uses-feature
    android:name="android.hardware.vr.headtracking"
    android:required="true"
  />
  <uses-feature
    android:name="oculus.software.handtracking"
    android:required="false"
  />
  <uses-permission android:name="com.oculus.permission.HAND_TRACKING" />
  <!-- Experimental extensions are optional for 2D panel apps -->
  <uses-feature
    android:name="com.oculus.experimental.enabled"
    android:required="false"
  />
  <!-- Tell the system this app can render passthrough -->
  <uses-feature
    android:name="com.oculus.feature.PASSTHROUGH"
    android:required="false"
  />
  <!-- Tell the system this app uses the virtual keyboard extensions -->
  <uses-feature
    android:name="com.oculus.feature.VIRTUAL_KEYBOARD"
    android:required="false"
  />
  <uses-feature android:glEsVersion="0x00030001" />
  <uses-feature
    android:name="oculus.software.overlay_keyboard"
    android:required="false"
  />
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-feature
    android:name="com.oculus.feature.RENDER_MODEL"
    android:required="false"
  />
  <uses-permission android:name="com.oculus.permission.RENDER_MODEL" />
  <!-- Volume Control -->
  <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

  <!-- WiFi Transfer Feature -->
  <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

  <!-- MediaStore Auto-Scan Feature (Android 14+) -->
  <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
  <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
  <application
    android:name=".OnTheGoVRApp"
    android:allowBackup="false"
    android:label="@string/app_name"
    android:networkSecurityConfig="@xml/network_security_config">

    <!-- Disable default WorkManager initialization so we can use HiltWorkerFactory -->
    <provider
      android:name="androidx.startup.InitializationProvider"
      android:authorities="${applicationId}.androidx-startup"
      android:exported="false"
      tools:node="merge">
      <meta-data
        android:name="androidx.work.WorkManagerInitializer"
        android:value="androidx.startup"
        tools:node="remove" />
    </provider>
    <meta-data
      android:name="com.oculus.supportedDevices"
      android:value="quest2|questpro|quest3"
    />
    <meta-data
      android:name="com.oculus.handtracking.version"
      android:value="V2.0"
    />
    <meta-data android:name="com.oculus.vr.focusaware" android:value="true" />
    <!-- Native library for Spatial SDK -->
    <uses-native-library
      android:name="libossdk.oculus.so"
      android:required="true"
    />
    <!-- Immersive VR Theatre Activity -->
    <!-- Intent filter is defined in flavor-specific manifest (immersive/panel) -->
    <activity
      android:name="com.inotter.onthegovr.ImmersiveActivity"
      android:launchMode="singleTask"
      android:excludeFromRecents="false"
      android:configChanges="screenSize|screenLayout|orientation|keyboardHidden|keyboard|navigation|uiMode"
      android:exported="false"
      android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
    />

    <!-- 2D panel activity -->
    <!-- Intent filter is defined in flavor-specific manifest (immersive/panel) -->
    <activity
      android:name="com.inotter.onthegovr.MainActivity"
      android:launchMode="singleTask"
      android:excludeFromRecents="false"
      android:configChanges="screenSize|screenLayout|orientation|keyboardHidden|keyboard|navigation|uiMode"
      android:exported="false"
      android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
    />

    <!-- WiFi Transfer Service -->
    <service
      android:name=".TransferService"
      android:foregroundServiceType="dataSync"
      android:exported="false"
    />

    <!-- Panel Activities (run in separate processes for Hilt compatibility) -->
    <activity
      android:name=".spatial.panels.LibraryPanelActivity"
      android:allowEmbedded="true"
      android:exported="false"
      android:process=":library_panel"
      android:theme="@style/PanelAppThemeTransparent"
    />
    <activity
      android:name=".spatial.panels.ControlsPanelActivity"
      android:allowEmbedded="true"
      android:exported="false"
      android:process=":controls_panel"
      android:theme="@style/PanelAppThemeTransparent"
    />
  </application>
</manifest>
