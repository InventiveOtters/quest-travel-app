<?xml version="1.0" encoding="utf-8" ?>
<manifest
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools"
>
  <!-- Panel flavor: MainActivity is the launcher (2D mode) -->

  <!-- For 2D panel mode, VR head tracking is NOT required -->
  <uses-feature
    android:name="android.hardware.vr.headtracking"
    android:required="false"
    tools:replace="android:required"
  />

  <application>
    <!-- MainActivity is the launcher in 2D panel mode -->
    <activity
      android:name="com.inotter.travelcompanion.MainActivity"
      android:exported="true"
      tools:replace="android:exported"
    >
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <!-- Set default panel size for 2D mode -->
      <layout
        android:defaultHeight="640dp"
        android:defaultWidth="1024dp" />
    </activity>

    <!-- ImmersiveActivity is NOT the launcher in 2D panel mode -->
    <activity
      android:name="com.inotter.travelcompanion.ImmersiveActivity"
      android:exported="false"
      tools:replace="android:exported"
    />
  </application>
</manifest>

