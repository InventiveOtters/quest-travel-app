<?xml version="1.0" encoding="utf-8" ?>
<manifest
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools"
>
  <!--
    Panel flavor: MainActivity is the launcher.
    User can switch to Immersive mode from Settings.
  -->

  <!-- For 2D panel mode, VR head tracking is NOT required -->
  <uses-feature
    android:name="android.hardware.vr.headtracking"
    android:required="false"
    tools:replace="android:required"
  />

  <application>
    <!-- MainActivity is the launcher - handles onboarding and routes to correct mode -->
    <activity
      android:name="com.inotter.onthegovr.MainActivity"
      android:exported="true"
      tools:replace="android:exported"
    >
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <!-- Set default panel size for 2D mode -->
      <layout
        android:defaultHeight="640dp"
        android:defaultWidth="1024dp" />
    </activity>

    <!-- ImmersiveActivity can be launched when user switches to Immersive mode -->
    <activity
      android:name="com.inotter.onthegovr.ImmersiveActivity"
      android:exported="true"
      tools:replace="android:exported"
    >
      <!-- Add VR category so it runs in immersive mode when launched -->
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="com.oculus.intent.category.VR" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
    </activity>
  </application>
</manifest>

